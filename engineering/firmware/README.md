# Firmware

Custom embedded code — currently not needed as we're using [Battery-Emulator](https://github.com/dalathegreat/Battery-Emulator/) unmodified.

Future candidates:
- X9C503S digipot control (closed-loop precharge voltage tracking)
- Any Battery-Emulator PRs we develop
